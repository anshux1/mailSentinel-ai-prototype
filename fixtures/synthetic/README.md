# Synthetic Phase 3 fixture

`phase3-minimal.eml` is a deliberately small, non-sensitive RFC 822-style
message. It is used for upload, storage and queue transport checks only. Phase
3 does not parse its headers or produce a verdict. The fixture was authored for
this repository and contains `.test` addresses and no real personal data.
